import { Button } from "@radix-ui/themes";
import { AppTheme } from "~/providers/AppTheme";

export const Attempt = () => {
  return (
    <AppTheme>
      <Button>Hello</Button>
    </AppTheme>
  );
};
